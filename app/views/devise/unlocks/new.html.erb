<div class="row">
  <div class="col-md-6 ml-auto mr-auto main-content px-5">
    <h2 class="text-center mb-3">Resend unlock instructions</h2>

    <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
      <%= devise_error_messages! %>

      <div class="form-group">
        <div class="input-group">
          <div class="input-group-prepend">
            <span class="input-group-text"><%= icon('fas', 'envelope') %></span>
          </div>
          <%= f.email_field :email, autofocus: true, class: 'form-control', placeholder: :email%>
        </div>
      </div>

      <div class="form-group">
        <%= f.submit "Resend unlock instructions", class: 'btn btn-primary btn-lg btn-block' %>
      </div>
    <% end %>

    <div class="text-center">
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>